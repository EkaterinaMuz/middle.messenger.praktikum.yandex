<main id="container" class="{{ styles.card }}">
    <form class="{{ styles.loginForm }}">
        <div class="{{ styles.fieldset }}">
            <legend class="{{ styles.legend }}">{{ formName }}</legend>
            {{{ fields }}}
        </div>
        {{{ Button }}}
    </form>
</main>

